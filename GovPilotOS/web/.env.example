# Base URL of the GovPilot OS Express backend (server/index.js)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Set to "true" to use built-in mock data instead of the live backend.
NEXT_PUBLIC_USE_MOCK=true
